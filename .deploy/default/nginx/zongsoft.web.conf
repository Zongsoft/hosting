# The api(back-end) of development environment configuration for the Nginx

# default site
server {
    listen       8080;
    listen       [::]:8080;
    server_name  _;

    location / {
        proxy_pass         http://127.0.0.1:8069/;
    }
}
